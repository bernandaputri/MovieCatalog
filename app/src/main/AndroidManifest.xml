<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.putri.moviesubmission">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_watcha"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_watcha_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieSubmission">
        <activity
            android:name="com.putri.moviesubmission.ui.detail.DetailActivity"
            android:label="@string/title_activity_detail"
            android:parentActivityName=".ui.main.MainActivity"
            android:theme="@style/Theme.MovieSubmission.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.main.MainActivity" />
        </activity>
        <activity
            android:name=".ui.splashscreen.SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.MainActivity" />
    </application>

</manifest>